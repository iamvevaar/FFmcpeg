# Screenshots

Drop the four product screenshots here, named exactly:

| File | Used in |
|---|---|
| `home.png` | Hero device mockup |
| `ai-mode.png` | "Talk to it like a person" section |
| `manual-mode.png` | "Or dial in every detail" section |
| `settings.png` | "Local-first. Always." section |

**Recommended:** PNG, ~2x retina (e.g., 2880×1800 if the app window is 1440×900). The page wraps each in a macOS-style window chrome, so capture the **app content only** — no OS chrome.

If a file is missing, the section renders a styled placeholder until you add it.
